package com.example.learn.features.main.presentation.interfaces;

import androidx.fragment.app.Fragment;

public interface OnFragmentActionListener {
    void switchFragment(Fragment targetFragment);
}

